import { FormatAudioDurationPipe } from './format-audio-duration.pipe';

describe('FormatAudioDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAudioDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
